language: node_js

sudo: required

services:
  - docker

cache: yarn

before_install:
  - npm i nsp -g
  - npm i snyk -g

before_script:
  - snyk auth ${SNYK_TOKEN} -d
  - snyk protect

after_success:
  - snyk monitor
  - ./deploy